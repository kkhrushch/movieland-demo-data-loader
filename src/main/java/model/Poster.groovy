package model

class Poster {
    String movieName
    String posterUrl

    Poster(String movieName, String posterUrl) {
        this.movieName = movieName
        this.posterUrl = posterUrl
    }
}
